import Sidebar = require("./components/soc-sidebar");
import Panel = require("./components/soc-panel");

class SocCore {
    constructor () {
        let s = new Sidebar();
    }
} 

let app = new SocCore();